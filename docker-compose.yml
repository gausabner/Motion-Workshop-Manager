services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: motion_app
    ports:
      - "5432:5432"
    volumes:
      - motion_pgdata:/var/lib/postgresql/data

volumes:
  motion_pgdata:
