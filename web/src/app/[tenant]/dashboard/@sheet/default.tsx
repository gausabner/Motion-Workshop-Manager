/**
 * Nothing, when no sheet is open.
 *
 * A parallel route needs a default or every navigation that does not match the
 * slot 404s the whole page.
 */
export default function NoSheet() {
    return null;
}
